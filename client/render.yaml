services:
  - type: web
    name: my-react-app
    env: static
    buildCommand: npm run build
    staticPublishPath: dist
    redirects:
      - source: /*
        destination: /index.html
        status: 200
